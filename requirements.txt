requests
beautifulsoup4
playwright
praw
python-dotenv
sentence-transformers
psycopg[binary]
psycopg_pool
pgvector
fastapi
uvicorn[standard]
# X/Twitter scraping is not yet functional -- see CLAUDE.md's "RAG sources
# status" section for what was tried (twikit, twifork, twscrape, direct
# Playwright login) and why each failed. No package listed here for it yet;
# add back once a working approach is found.
